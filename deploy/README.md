# Performance deployment snippets

- `nginx-performance.conf`: use inside `server {}` when deploying with Nginx.
- `Caddyfile`: standalone example for Caddy.

Both files enable compression and cache headers tailored for static VuePress output.
