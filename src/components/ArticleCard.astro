---
import { formatDate } from "../lib/content";

interface Props {
  href: string;
  title: string;
  description?: string;
  date: Date | string;
  category?: string;
  tags?: string[];
}

const { href, title, description, date, category, tags = [] } = Astro.props;
---

<a class="article-card" href={href} data-category={category ?? ""} data-tags={JSON.stringify(tags)}>
  <div class="meta">
    <time datetime={new Date(date).toISOString()}>{formatDate(date)}</time>
    {category && <span>{category}</span>}
  </div>
  <div>
    <h3>{title}</h3>
    {description && <p>{description}</p>}
  </div>
</a>
